from django.urls import path
from . import views

app_name = 'reports'

urlpatterns = [
    # URLs para reportes - a implementar más tarde
    # path('', views.report_list, name='report_list'),
    # path('request/', views.request_report, name='request_report'),
]