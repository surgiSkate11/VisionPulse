from django.urls import path
from . import views

app_name = 'exercises'

urlpatterns = [
    # URLs para ejercicios - a implementar más tarde
    # path('', views.exercise_list, name='exercise_list'),
    # path('<int:exercise_id>/', views.exercise_detail, name='exercise_detail'),
]